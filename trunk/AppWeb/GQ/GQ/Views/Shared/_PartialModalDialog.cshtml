<script type="text/ng-template" id="modalOkCancel.html">
    <div class="modal-header">
        <h3 class="modal-title">
            <i class="fa {{data.iconoTituloModal}}"></i>
            <span class="modal-title" ng-bind-html="deliberatelyTrustDangerousSnippet(data.titulo)"></span>
        </h3>
    </div>
    <div class="modal-body" ng-bind-html="deliberatelyTrustDangerousSnippet(data.mensaje)">
    </div>
    <div class="modal-footer">
        <button ng-class="{'btn btn-outline':(data.aplicarOutline), 'btn btn-primary':(!data.aplicarOutline)}" type="button" ng-click="onOk()">@Localizer["default_acept"]</button>
        <button ng-class="{'btn btn-outline':(data.aplicarOutline), 'btn btn-warning':(!data.aplicarOutline)}" type="button" ng-click="onCancel()">@Localizer["default_cancel"]</button>
    </div>
</script>

<script type="text/ng-template" id="modalOk.html">
    <div class="modal-header">
        <h3 class="modal-title">
            <i class="fa {{data.iconoTituloModal}}"></i>
            <span class="modal-title" ng-bind-html="deliberatelyTrustDangerousSnippet(data.titulo)"></span>
        </h3>
    </div>
    <div class="modal-body" ng-bind-html="deliberatelyTrustDangerousSnippet(data.mensaje)">
    </div>
    <div class="modal-footer">
        <button ng-class="{'btn btn-outline':(data.aplicarOutline), 'btn btn-primary':(!data.aplicarOutline)}" type="button" ng-click="onOk()">@Localizer["default_acept"]</button>
    </div>
</script>

<script type="text/ng-template" id="modalYesNo.html">
    <div class="modal-header">
        <h3 class="modal-title">
            <i class="fa {{data.iconoTituloModal}}"></i>
            <span class="modal-title" ng-bind-html="deliberatelyTrustDangerousSnippet(data.titulo)"></span>
        </h3>
    </div>
    <div class="modal-body" ng-bind-html="deliberatelyTrustDangerousSnippet(data.mensaje)">
    </div>
    <div class="modal-footer">
        <button ng-class="{'btn btn-outline':(data.aplicarOutline), 'btn btn-primary':(!data.aplicarOutline)}" type="button" ng-click="onOk()">@Localizer["default_yes"]</button>
        <button ng-class="{'btn btn-outline':(data.aplicarOutline), 'btn btn-warning':(!data.aplicarOutline)}" type="button" ng-click="onCancel()">@Localizer["default_no"]</button>
    </div>
</script>





